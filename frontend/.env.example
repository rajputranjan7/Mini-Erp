# Copy to .env for local dev. Point this at your deployed backend in production
# (e.g. set VITE_API_URL on Vercel/Netlify to your Render/Railway backend URL + /api).
VITE_API_URL=http://localhost:4000/api
